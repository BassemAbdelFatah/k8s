apiVersion: apps/v1
kind: Deployment
metadata:
  name: consumer
  namespace: consumer-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: consumer
  template:
    metadata:
      labels:
        app: consumer
    spec:
      #      hostNetwork: true
      #      dnsPolicy: "None"
      #      dnsConfig:
      #        nameservers:
      #         - 8.8.8.8
      #         - 10.96.0.10
      #        searches:
      #          - consumer-prod.svc.cluster.local
      #          - svc.cluster.local
      #          - cluster.local
      containers:
        - name: consumer
          image: 10.133.70.221:5000/consumerapi-prod:106
          imagePullPolicy: IfNotPresent
          env:
            - name: TZ
              value: "Africa/Cairo"
          #        volumeMounts:
          #        - mountPath: "/var/www/Logs/Consumer"
          #          name: backend-consumer
          ports:
            - containerPort: 8070
          resources:
              requests:
                memory: "360Mi"
                cpu: "50m"
              limits:
                memory: "520Mi"
                cpu: "200m"
      #      nodeName: consumer-test-backend1
      #      nodeName: consumer-test-backend2
      #      volumes:
      #       - name: backend-consumer
      #         persistentVolumeClaim:
      #          claimName: claim-backend
      imagePullSecrets:
        - name: myregistrykey
#      affinity:
#        nodeAffinity:
#          requiredDuringSchedulingIgnoredDuringExecution:
#            nodeSelectorTerms:
#              - matchExpressions:
#                  - key: kubernetes.io/hostname
#                    operator: In
#                    values:
#                      - k8s-con-wrkr-03.momkndc.corp
---
apiVersion: v1
kind: Service
metadata:
  name: consumer
  namespace: consumer-prod
spec:
  ports:
    - name: http-consumer
      targetPort: 8070
      port: 8070
      nodePort: 31070
  selector:
    app: consumer
  type: NodePort
#---
#apiVersion: v1
#kind: Service
#metadata:
#  name: consumer-service
#  namespace: consumer-prod
#spec:
#  selector:
#    app: consumer
#  ports:
#    - protocol: TCP
#      port: 80
#      targetPort: 8070
